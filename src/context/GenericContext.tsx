// import React, { useContext } from "react";

// export const GenericContext = React.createContext({});

// export const useGenericContext = () => useContext(GenericContext);

// //Screens that will always expect a dynamic name provided after being loaded
// const dynamicScreens = ["ProfileDetails", "JobPreview", "OrganizationScreen"];

// export const GenericContextProvider = ({ children }) => {
//   return (
//     <GenericContext.Provider value={{}}>{children}</GenericContext.Provider>
//   );
// };
